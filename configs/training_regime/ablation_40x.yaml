# @package _global_
# 40 tokens/param dev regime — for 600M daily iteration (SPEC.md §1.2).
training:
  tokens_per_param: 40
  global_batch_size: 1024
  micro_batch_size: null

checkpointing:
  save_every_tokens: 2_000_000_000
  keep_last: 3
  save_stable_stage_final: true
